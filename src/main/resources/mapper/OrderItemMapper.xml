<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ssmshop.mapper.OrderItemMapper">
    <insert id="insert" parameterType="OrderItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_items (order_id, product_id, product_name, cover_url, unit_price, quantity, subtotal)
        VALUES (#{orderId}, #{productId}, #{productName}, #{coverUrl}, #{unitPrice}, #{quantity}, #{subtotal})
    </insert>

    <select id="findByOrderId" resultType="OrderItem">
        SELECT * FROM order_items WHERE order_id = #{orderId} ORDER BY id ASC
    </select>
</mapper>
